apply plugin: 'application'

mainClassName = 'org.itsallcode.whiterabbit.textui.App'

dependencies {
    compile project(':logic')
}

// Avoid gradle deprecation warning
compileJava {
    options.compilerArgs += '-proc:none'
}
